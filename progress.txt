# Progress Log

## Learnings
(Notes discovered during implementation)

## Iteration 1 — US-001: Project Scaffolding
- What was done: Created monorepo structure with backend/ and frontend/ directories, all dependency files, root config files
- Files affected:
  - backend/pyproject.toml, requirements.txt, requirements-dev.txt
  - frontend/package.json, tsconfig.json, src/main.tsx (placeholder)
  - .gitignore, .env.example, docker-compose.yml, Makefile
- Learnings for next cycles:
  - Patterns: pip install and npm install both succeed cleanly on this machine
  - Traps: frontend needs tsconfig.json + at least one .tsx file for typecheck to have something to check
  - Helpful context: Python 3.x and Node available in PATH; using asyncpg 0.31, SQLAlchemy 2.0.48, FastAPI 0.135
---

## Iteration 2 — US-002: Backend skeleton with health endpoint
- What was done: Created FastAPI app with health check, CORS middleware, lifespan hook, Pydantic Settings config, structlog JSON logging setup
- Files affected:
  - backend/app/__init__.py (new)
  - backend/app/main.py (new) — FastAPI app with create_app factory, CORS, lifespan, /health endpoint
  - backend/app/config.py (new) — Pydantic Settings with all env vars from .env.example
  - backend/app/core/__init__.py (new)
  - backend/app/core/logging.py (new) — structlog setup with JSON/console renderer switch
  - backend/tests/__init__.py (new)
  - backend/tests/test_health.py (new) — async test using httpx ASGITransport
- Learnings for next cycles:
  - Patterns: Use httpx AsyncClient with ASGITransport for testing FastAPI without running a server; pytest-anyio works with the asyncio_mode="auto" config
  - Traps: pytest and dev deps were not installed in the conda env despite requirements-dev.txt existing; need to pip install them first
  - Helpful context: conda env is "smart-shopping"; mypy strict mode passes with --ignore-missing-imports; structlog async logging uses ainfo/adebug etc.
---

## Iteration 3 — US-003: Frontend skeleton with RTL and phone frame
- What was done: Created full frontend app shell — Vite config, Tailwind config, PostCSS config, index.html with dir="rtl" lang="he", React Router setup in main.tsx/App.tsx, AppShell with phone frame (430px max-width centered with rounded corners on desktop, full screen on mobile), BottomNav with 3 tabs (רשימה, קבלות, עוד) using lucide-react icons, and 3 placeholder pages.
- Files affected:
  - frontend/index.html (new) — RTL Hebrew entry point
  - frontend/vite.config.ts (new) — Vite + React plugin + path alias
  - frontend/tailwind.config.ts (new) — Tailwind with custom phone max-width and Hebrew font stack
  - frontend/postcss.config.js (new) — PostCSS with Tailwind + autoprefixer
  - frontend/src/index.css (new) — Tailwind directives + base styles
  - frontend/src/main.tsx (updated) — React 18 createRoot with BrowserRouter
  - frontend/src/App.tsx (new) — Routes for /, /list, /receipts, /more
  - frontend/src/components/AppShell.tsx (new) — Phone frame wrapper with scrollable content
  - frontend/src/components/BottomNav.tsx (new) — 3-tab bottom navigation with active state
  - frontend/src/pages/ListPage.tsx (new) — Placeholder list page
  - frontend/src/pages/ReceiptsPage.tsx (new) — Placeholder receipts page
  - frontend/src/pages/MorePage.tsx (new) — Placeholder more page
- Learnings for next cycles:
  - Patterns: Vite 6.x works well with @vitejs/plugin-react 4.x; tsconfig path aliases need matching vite resolve.alias; Tailwind 3.4 with content glob picks up all tsx files
  - Traps: index.html must be at frontend root (not in public/) for Vite; pb-safe utility useful for iPhone bottom safe area but needs custom definition or env support
  - Helpful context: Phone frame uses sm:max-w-phone (430px custom) + sm:rounded-3xl + shadow-2xl for Apple-like appearance; BottomNav uses absolute positioning at bottom with backdrop-blur; lucide-react provides ClipboardList, Receipt, MoreHorizontal icons
---

## Iteration 4 — US-004: Database models with UUID primary keys
- What was done: Created all SQLAlchemy 2.0 async models with UUID PKs, timestamp mixins, async session factory. 8 models: User, Category, Product, ListItem, Receipt, Purchase, PriceHistory, UserProductPreference. ListItem has all required fields (status, refresh fields, source, confidence, display_order). All relationships and FKs defined with proper cascade rules. Composite indexes for key query patterns (user+status, refresh engine, price lookups).
- Files affected:
  - backend/app/db/__init__.py (new)
  - backend/app/db/session.py (new) — async engine, session factory, get_db dependency with commit/rollback
  - backend/app/models/__init__.py (new) — re-exports all models
  - backend/app/models/base.py (new) — Base, UUIDMixin (gen_random_uuid), TimestampMixin (func.now)
  - backend/app/models/user.py (new) — User with Google OAuth fields
  - backend/app/models/category.py (new) — user-scoped categories with unique constraint
  - backend/app/models/product.py (new) — global products with barcode + normalized_name indexes
  - backend/app/models/list_item.py (new) — core entity with status cycling, refresh fields, enums
  - backend/app/models/receipt.py (new) — Receipt + Purchase models with JSON parsed data
  - backend/app/models/price_history.py (new) — price tracking with composite index
  - backend/app/models/user_product_preference.py (new) — per-user product preferences
- Learnings for next cycles:
  - Patterns: SQLAlchemy 2.0 Mapped[] + mapped_column() works well with mypy strict; use `from __future__ import annotations` + TYPE_CHECKING for forward refs to avoid circular imports; String columns with Python enums preferred over PG native enums for migration flexibility
  - Traps: mypy strict requires `dict[str, Any]` not `dict` for JSON columns; `type: ignore[assignment]` not needed when proper type annotation used
  - Helpful context: Tables created: users, categories, products, list_items, receipts, purchases, price_history, user_product_preferences; categories are user-scoped (each user gets own copy); products are global (shared across users)
---

## Iteration 5 — US-005: Alembic setup and initial migration with seed categories
- What was done: Initialized Alembic with async template, configured env.py to read DATABASE_URL from app.config.Settings, updated alembic.ini to skip hardcoded URL. Created manual migration 0001_initial_schema.py with all 8 tables (users, categories, products, list_items, receipts, purchases, price_history, user_product_preferences) including indexes, constraints, and proper FK cascade rules. Created seed script at app/db/seed.py with 15 default Hebrew categories and seed_categories_for_user() function for programmatic use.
- Files affected:
  - backend/alembic.ini (new) — Alembic config, URL set programmatically
  - backend/alembic/env.py (new) — async env with app models imported for autogenerate
  - backend/alembic/versions/0001_initial_schema.py (new) — full initial schema migration
  - backend/alembic/script.py.mako (new) — Alembic template for future migrations
  - backend/alembic/README (new) — Alembic boilerplate
  - backend/app/db/seed.py (new) — 15 Hebrew categories seed with idempotent insert
- Learnings for next cycles:
  - Patterns: Manual migration writing avoids need for live DB during autogenerate; `alembic upgrade head --sql` validates SQL generation offline; seed_categories_for_user() is idempotent (skips existing names) and should be called on new user creation
  - Traps: `alembic revision --autogenerate` requires a live database connection even for initial schema; Windows console can't display Hebrew in python print — use file I/O or redirect to test Hebrew content; no .env file exists yet so no live DB tests possible
  - Helpful context: conda env is "smart-shopping"; alembic is already in requirements.txt; migration uses gen_random_uuid() server_default matching the UUIDMixin; seed script can be run as `python -m app.db.seed <user_id>` or imported programmatically
---

## Iteration 6 — US-006: Error handling system
- What was done: Created structured error hierarchy with SmartKalError base class and 9 subclasses (ValidationError, AuthenticationError, NotFoundError, RateLimitError, ExternalServiceError, ReceiptParsingError, ClaudeAPIError, SuperGETError, DatabaseError). Each error has error_code, message_he, message_en, status_code, details dict, and auto-captured source_location. Added RequestIDMiddleware that injects X-Request-ID into every request/response. Added global exception handlers for SmartKalError and unhandled Exception. Error response format: {error: {code, message, message_en, details, debug: {timestamp, request_id, source}}}. 21 tests covering all error types, integration tests via test app routes, request ID passthrough, and health endpoint regression.
- Files affected:
  - backend/app/core/errors.py (new) — SmartKalError hierarchy with 9 subclasses, Hebrew messages, to_dict() serialization
  - backend/app/core/middleware.py (new) — RequestIDMiddleware with X-Request-ID injection and unhandled exception catch
  - backend/app/core/exception_handlers.py (new) — Global handlers for SmartKalError and Exception, register_exception_handlers()
  - backend/app/main.py (updated) — Added RequestIDMiddleware and exception handler registration
  - backend/tests/test_errors.py (new) — 21 tests: parametrized error class tests, integration tests, request ID tests
- Learnings for next cycles:
  - Patterns: Use parametrized pytest tests for exhaustive error class coverage; create isolated test FastAPI apps with test routes for integration testing error handling
  - Traps: FastAPI's add_exception_handler(Exception, ...) does NOT catch exceptions that bubble through BaseHTTPMiddleware's call_next — must catch unhandled exceptions directly in the middleware dispatch method; mypy type: ignore comments may become unused across versions — use [arg-type, unused-ignore] combo
  - Helpful context: ClaudeAPIError and SuperGETError inherit from ExternalServiceError (all 502); ReceiptParsingError is 422 not 502 since it's a client-side parsing issue; structlog contextvars binding in middleware ensures request_id appears in all log entries automatically
---

## Iteration 7 — US-007: Google OAuth + JWT authentication backend
- What was done: Implemented full Google OAuth + JWT authentication system. Created security module with Google id_token verification (using google-auth library's verify_oauth2_token), JWT access token (15min) and refresh token (30d) creation/decoding with type validation. Created get_current_user FastAPI dependency with HTTPBearer scheme. Built 3 auth endpoints: POST /api/v1/auth/google (login/register with Google, seeds categories for new users), POST /api/v1/auth/refresh (exchange refresh for new pair), GET /api/v1/auth/me (protected, returns user profile). Set up API router structure (app/api/v1/). 25 tests covering JWT lifecycle (10), Google verification (5), and endpoint integration (10).
- Files affected:
  - backend/requirements.txt (updated) — added google-auth>=2.38.0, requests>=2.32.0
  - backend/app/core/security.py (new) — verify_google_token, create_access_token, create_refresh_token, create_token_pair, decode_token
  - backend/app/dependencies.py (new) — get_current_user with HTTPBearer + JWT validation + DB lookup
  - backend/app/api/__init__.py (new)
  - backend/app/api/v1/__init__.py (new) — api_v1_router with /api/v1 prefix
  - backend/app/api/v1/auth.py (new) — GoogleLoginRequest/TokenResponse/RefreshRequest/UserResponse schemas + 3 endpoints
  - backend/app/main.py (updated) — added api_v1_router include
  - backend/tests/test_auth.py (new) — 25 tests: JWT lifecycle, Google token verification, auth endpoint integration
- Learnings for next cycles:
  - Patterns: google-auth library's verify_oauth2_token is the canonical way to verify Google id_tokens; use HTTPBearer(auto_error=False) to handle missing auth header gracefully in dependency; db.flush() after add() gets the DB-assigned UUID without committing
  - Traps: google-auth requires `requests` package for its transport layer (not installed automatically); patching the User model class in tests breaks SQLAlchemy's select() — instead use mock_session.add.side_effect to capture and modify the User object; jwt.encode returns Any in python-jose — need explicit `token: str` annotation for mypy strict
  - Helpful context: API routes live under app/api/v1/ with api_v1_router aggregating all sub-routers; dependency_overrides on FastAPI app is the clean way to mock DB sessions in integration tests; new users automatically get 15 Hebrew categories seeded via seed_categories_for_user
---

## Iteration 8 — US-008: Frontend auth with Google Sign-In and Axios interceptor
- What was done: Created full frontend auth system. Zustand auth store (authStore.ts) stores tokens in memory only (no localStorage). Axios client (api/client.ts) with request interceptor to attach Bearer token and response interceptor for auto-refresh on 401 with request coalescing. Auth bridge pattern breaks circular dependency between store and client. OnboardingPage with Google Identity Services (GIS) integration — renders Google Sign-In button via google.accounts.id.renderButton(). Toast component for Hebrew error messages mapped from backend error codes. App.tsx updated with auth routing — unauthenticated users see OnboardingPage, authenticated users see main app. Added vite-env.d.ts for Vite client types. GIS script loaded in index.html.
- Files affected:
  - frontend/src/api/client.ts (new) — Axios instance, JWT interceptor, 401 auto-refresh, error code → Hebrew mapping, auth bridge
  - frontend/src/store/authStore.ts (new) — Zustand store with loginWithGoogle, refreshTokens, fetchMe, logout, initialize
  - frontend/src/components/Toast.tsx (new) — Toast notification system with showToast() for non-React code
  - frontend/src/pages/OnboardingPage.tsx (new) — Google Sign-In via GIS, branding, error handling
  - frontend/src/App.tsx (updated) — Auth-gated routing with initializing spinner
  - frontend/index.html (updated) — Added Google Identity Services script tag
  - frontend/src/vite-env.d.ts (new) — Vite client type reference for import.meta.env
- Learnings for next cycles:
  - Patterns: Use registerAuthBridge() pattern to break circular dependency between Axios client and Zustand store; Google Identity Services (GIS) uses google.accounts.id.initialize() + renderButton() — no redirect flow needed; coalesce concurrent 401 refresh attempts with a shared promise
  - Traps: Vite projects need vite-env.d.ts (or reference in tsconfig) for import.meta.env types; GIS script loads async so need polling/interval to detect when window.google is available; don't attempt token refresh on /auth/ endpoints to avoid infinite loops
  - Helpful context: VITE_GOOGLE_CLIENT_ID env var controls Google Sign-In; tokens are memory-only so page reload = logout (by design for security); the toast system exposes showToast() for imperative use from interceptors
---

## Iteration 9 — US-009: Shopping list CRUD backend
- What was done: Created shopping list CRUD API with 4 endpoints: GET /api/v1/list (items grouped by category), POST /api/v1/list/items (add with optional auto-categorization), PUT /api/v1/list/items/{id} (update name/quantity/note/category), DELETE /api/v1/list/items/{id} (permanent remove). Created auto-categorization service using Claude API (AsyncAnthropic) that matches Hebrew product names to user's categories. All endpoints are user-scoped via get_current_user dependency. 23 tests covering CRUD, user isolation, auto-categorization (mocked), auth protection, and edge cases.
- Files affected:
  - backend/app/services/__init__.py (new)
  - backend/app/services/categorizer.py (new) — Claude API auto-categorization with graceful fallback
  - backend/app/api/v1/list.py (new) — 4 CRUD endpoints with Pydantic schemas, category grouping
  - backend/app/api/v1/__init__.py (updated) — added list_router
  - backend/tests/test_list.py (new) — 23 tests across 7 test classes
- Learnings for next cycles:
  - Patterns: Override get_current_user dependency directly in tests to avoid JWT complexity for non-auth tests; use side_effect on mock_session.execute to return different results for sequential queries; Anthropic SDK content blocks are a union type — need type: ignore[union-attr] for .text access
  - Traps: mypy strict flags accessing .text on Anthropic response content blocks since it's a union of TextBlock and many other types; cat_map.get() with UUID | None key needs `is not None` guard not truthiness check
  - Helpful context: List items grouped by category for frontend rendering; auto-categorization is best-effort (returns None on any failure — no error thrown to user); the categorizer uses AsyncAnthropic client with claude-sonnet-4-20250514 model
---

## Iteration 10 — US-010: Complete/activate items + auto-refresh engine
- What was done: Created auto-refresh engine service (refresh_engine.py) with frequency calculation using median of intervals from completion history and receipt purchases. Added 4 new endpoints: PATCH complete (sets status=completed, calculates next_refresh_at), PATCH activate (sets status=active, clears next_refresh_at), POST refresh (activates all overdue completed items), PATCH preferences (user override for auto_refresh_days). Confidence scoring tiers: 1→0.2, 2→0.3, 3-4→0.4, 5-9→0.6, 10+→0.8, low variance bonus +0.15 (CV<0.3), user override=0.95. 44 new tests (114 total) covering: pure function unit tests (compute_refresh_days, calculate_confidence, timestamps_to_intervals), endpoint integration tests (complete/activate/refresh/preferences with success/error cases), auth protection for all new endpoints, activate_overdue_items service tests, frequency calculation with known weekly/biweekly date patterns.
- Files affected:
  - backend/app/services/refresh_engine.py (new) — frequency calculation, confidence scoring, overdue activation
  - backend/app/api/v1/list.py (updated) — 4 new endpoints + PreferencesRequest/RefreshResponse schemas
  - backend/tests/test_refresh.py (new) — 44 tests across 10 test classes
- Learnings for next cycles:
  - Patterns: Patch imported functions in the module where they're used (e.g., patch("app.api.v1.list.calculate_refresh_for_item")) for clean endpoint testing; statistics.median/stdev from stdlib sufficient for interval calculations; coefficient of variation (stdev/mean) is a good variance metric for the low-variance bonus
  - Traps: mypy strict on SQLAlchemy <= comparison with nullable DateTime columns may or may not need type: ignore[operator] depending on SQLAlchemy version — check before adding; __import__() inline is an anti-pattern, always use proper imports
  - Helpful context: Refresh engine gathers timestamps from both item completion history (last_completed_at) and receipt purchases (Purchase→Receipt join); activate_overdue_items uses ix_list_items_refresh index (status, next_refresh_at); user override auto_refresh_days on ListItem takes priority over system_refresh_days with 0.95 confidence
---

## Iteration 11 — US-011: Category management backend
- What was done: Created full category management API with 5 endpoints: GET /api/v1/categories (list all user categories ordered by display_order), POST /api/v1/categories (create with duplicate name check, auto display_order), PUT /api/v1/categories/{id} (rename/change icon with duplicate check excluding self), DELETE /api/v1/categories/{id} (moves items to "אחר" via bulk UPDATE, prevents deleting "אחר" itself, creates "אחר" if missing), POST /api/v1/categories/reorder (bulk reorder by passing ordered array of IDs). All endpoints user-scoped. 25 tests covering CRUD, reorder, delete with item reassignment, "אחר" creation on delete, user isolation, seed defaults (15 categories), and auth protection for all 5 endpoints. Seeding on new user creation was already implemented in auth.py (Iteration 7).
- Files affected:
  - backend/app/api/v1/category.py (new) — 5 endpoints with Pydantic schemas, helpers for category lookup and "אחר" fallback
  - backend/app/api/v1/__init__.py (updated) — added category_router
  - backend/tests/test_category.py (new) — 25 tests across 8 test classes
- Learnings for next cycles:
  - Patterns: Use SQLAlchemy update() for bulk operations (moving items on category delete) instead of loading each item individually; _get_or_create_other_category pattern ensures "אחר" always exists as a fallback; duplicate name checks need to exclude the current entity on update (Category.id != category_id)
  - Traps: The "אחר" category might not exist if user manually deleted it before this code was in place — always use get_or_create pattern; reorder endpoint should validate all IDs belong to the user before updating any display_order values
  - Helpful context: Category router registered at /api/v1/categories; 139 total tests pass (25 new + 114 existing); mypy strict passes; categories are user-scoped with UniqueConstraint on (user_id, name)
---

## Iteration 12 — US-012: Shopping list UI — basic rendering with categories
- What was done: Created three frontend components for the shopping list UI. ShoppingList.tsx defines shared TypeScript types (ListItemData, CategoryInfo, CategoryGroup, ListResponse) and renders items split into active groups by category plus a collapsible completed section. CategorySection.tsx renders a collapsible category header with chevron icon (lucide-react ChevronDown), bold category name, item count, and child ListItem components. ListItem.tsx renders circle indicators (empty circle for active, filled green with checkmark for completed), product name with line-through styling for completed items, and optional quantity badge. Updated ListPage.tsx to fetch from GET /api/v1/list on mount with loading spinner, error state, and data rendering.
- Files affected:
  - frontend/src/components/ShoppingList.tsx (new) — types + main list component with active/completed split
  - frontend/src/components/CategorySection.tsx (new) — collapsible category with chevron toggle
  - frontend/src/components/ListItem.tsx (new) — item row with circle indicator + quantity
  - frontend/src/pages/ListPage.tsx (updated) — fetch on mount, loading/error states, renders ShoppingList
- Learnings for next cycles:
  - Patterns: Export shared types from ShoppingList.tsx so sibling components (CategorySection, ListItem) can import them; use cancelled flag in useEffect cleanup to prevent state updates on unmounted components; split active/completed items client-side from the grouped API response
  - Traps: API returns items grouped by category with mixed statuses — need to filter active vs completed per group on the frontend; import default export from client.ts as `api` (lowercase)
  - Helpful context: ListResponse matches backend's ListResponse schema exactly; Apple Reminders style achieved with 22px circles, 15px text, gray-400 for completed items, ChevronDown with rotate transition; CompletedSection collapsed by default with show/hide toggle in Hebrew
---

## Iteration 13 — US-013: Shopping list UI — check/uncheck animations and completed section
- What was done: Added check/uncheck toggle functionality with CSS transition animations to the shopping list UI. ListItem.tsx now has an onToggle callback with optimistic visual flip — tapping the circle animates it between empty and filled green (with scale-110 pop effect) using 300ms CSS transitions on border, background, opacity, and scale. Item text transitions between normal and gray+line-through. The animation state is managed locally with useState, and the actual API call fires after the 300ms animation completes. The onToggle prop is threaded through ShoppingList → CategorySection → ListItem. ListPage.tsx handles the toggle by calling PATCH /api/v1/list/items/{id}/complete or /activate, then re-fetches the full list to update groupings. Used AbortController pattern instead of cancelled flag for cleaner fetch cleanup. CompletedSection already existed with show/hide toggle — now also receives onToggle so completed items can be reactivated.
- Files affected:
  - frontend/src/components/ListItem.tsx (updated) — added onToggle prop, local animation state, optimistic checked visual, CSS transition classes, accessible button with Hebrew aria-label
  - frontend/src/components/CategorySection.tsx (updated) — added onToggle prop passthrough to ListItem
  - frontend/src/components/ShoppingList.tsx (updated) — added onToggle prop passthrough to CategorySection and CompletedSection
  - frontend/src/pages/ListPage.tsx (updated) — added handleToggle with PATCH API calls and re-fetch, switched to AbortController, extracted fetchList as useCallback for reuse
- Learnings for next cycles:
  - Patterns: Optimistic animation via local state flip + setTimeout → API call gives instant feedback; AbortController is cleaner than cancelled flag for fetch cleanup; useCallback for fetchList enables reuse in both useEffect and event handlers
  - Traps: Don't use optimistic state updates on the data model when items move between sections (active↔completed) — re-fetching is more reliable since the server recalculates groupings and refresh timestamps
  - Helpful context: Transitions use Tailwind's transition-all duration-300; scale-110 on check gives Apple-like "pop" effect; circle button has aria-label in Hebrew for accessibility ("הפעל מחדש" / "סמן כהושלם")
---

## Iteration 14 — US-014: Shopping list UI — add item input with autocomplete
- What was done: Created AddItemInput.tsx with FAB (⊕) that opens inline text input with Hebrew autocomplete from product history. Added backend endpoint GET /api/v1/list/suggestions?q= that returns distinct item names matching the query, ordered by most recently used, deduped, limited to 10 results. AddItemInput calls POST /api/v1/list/items on submit (Enter key or green + button), supports selecting from autocomplete dropdown (which passes category_id for accurate re-categorization). Added green dot badge on ListItem.tsx for auto-refreshed items (source === "auto_refresh"). Updated ListPage.tsx to call POST /api/v1/list/refresh on mount (once per component lifecycle) before fetching the list, and integrated AddItemInput with onItemAdded callback that re-fetches the list. 4 new tests: suggestions with matches, empty query, deduplication, auth protection. Total: 143 tests passing.
- Files affected:
  - backend/app/api/v1/list.py (updated) — added SuggestionItem/SuggestionsResponse schemas, GET /suggestions endpoint with ilike search and Python-side deduplication
  - backend/tests/test_list.py (updated) — 4 new tests for suggestions endpoint
  - frontend/src/components/AddItemInput.tsx (new) — FAB button, inline text input, debounced autocomplete (200ms), suggestion dropdown, submit via Enter or button
  - frontend/src/components/ListItem.tsx (updated) — green dot badge for source === "auto_refresh"
  - frontend/src/pages/ListPage.tsx (updated) — POST /api/v1/list/refresh on mount, integrated AddItemInput with onItemAdded
- Learnings for next cycles:
  - Patterns: Use useRef(false) for one-time-per-mount effects (refreshedRef); debounce autocomplete with clearTimeout/setTimeout pattern in onChange; pass category_id from suggestions to avoid re-categorization on known items
  - Traps: Don't import `distinct` from sqlalchemy when deduplicating in Python — unused imports cause linting issues; FAB positioning with `fixed bottom-24 left-1/2 -translate-x-1/2` works for mobile but needs `sm:absolute` for phone-frame desktop layout
  - Helpful context: Suggestions endpoint uses ilike for case-insensitive contains match; AddItemInput keeps input open after submit for adding multiple items; auto-refresh badge uses `h-2 w-2 rounded-full bg-green-400` for a subtle green dot
---

## Iteration 15 — US-015: Shopping list UI — swipe to delete and item details
- What was done: Created SwipeableListItem.tsx wrapping ListItem with touch-based swipe-to-delete (swipe left reveals red "הסר" button, threshold 80px) and long-press detection (500ms) for opening item details. Created ItemDetailsSheet.tsx bottom sheet with quantity, note, and refresh frequency override fields. Bottom sheet saves via PUT /api/v1/list/items/{id} for item details and PATCH /api/v1/list/items/{id}/preferences for refresh override. Updated CategorySection, ShoppingList (including CompletedSection), and ListPage to thread through onDelete and onLongPress callbacks. Added slide-up animation keyframe to Tailwind config. Delete uses DELETE /api/v1/list/items/{id} endpoint. All 143 backend tests pass, TypeScript typecheck passes.
- Files affected:
  - frontend/src/components/SwipeableListItem.tsx (new) — touch swipe with RTL-aware left swipe, long-press timer, delete animation, mouse fallback for desktop
  - frontend/src/components/ItemDetailsSheet.tsx (new) — bottom sheet with quantity/note/refreshDays fields, backdrop click to close, saves item details + preferences
  - frontend/src/components/CategorySection.tsx (updated) — uses SwipeableListItem, passes onDelete/onLongPress
  - frontend/src/components/ShoppingList.tsx (updated) — threads onDelete/onLongPress through to CategorySection and CompletedSection
  - frontend/src/pages/ListPage.tsx (updated) — added handleDelete, handleLongPress, detailsItem state, ItemDetailsSheet integration
  - frontend/tailwind.config.ts (updated) — added slide-up keyframe animation
- Learnings for next cycles:
  - Patterns: Wrap ListItem in a SwipeableListItem for touch interactions without modifying the base component; use useRef for tracking touch coordinates to avoid re-renders during swipe; RTL swipe-left maps to positive deltaX (startX - currentX); separate timer ref for long-press with cleanup on touch move
  - Traps: In RTL layout, "end-0" positions to the left side (where delete button should appear after left swipe); must use `swiping ? "none" : transition` on transform to avoid laggy touch following; CSS overflow-hidden on container prevents delete button from showing outside bounds
  - Helpful context: DELETE_THRESHOLD = 80px for swipe activation; LONG_PRESS_MS = 500ms; animate-slide-up uses 250ms ease-out; bottom sheet max-width matches phone frame (430px); ItemDetailsSheet conditionally sends PUT only when quantity/note changed and PATCH only when refreshDays changed
---

## Iteration 16 — US-016: Price comparison card on list page
- What was done: Created PriceComparisonCard.tsx that fetches from GET /api/v1/prices/compare-list, renders a collapsible green card at the top of the shopping list showing the cheapest store and potential savings. Card is completely hidden when no price data exists (API returns error or empty data — card stays null). Tap expands to show per-store breakdown with matched product counts and totals. Coverage percentage shown ("השוואה על X מתוך Y מוצרים"). Green dot indicator marks the cheapest store. Integrated into ListPage.tsx between header and AddItemInput. TypeScript typecheck passes.
- Files affected:
  - frontend/src/components/PriceComparisonCard.tsx (new) — self-contained card component with fetch, expand/collapse, per-store breakdown
  - frontend/src/pages/ListPage.tsx (updated) — imported and placed PriceComparisonCard above AddItemInput
- Learnings for next cycles:
  - Patterns: Self-contained data-fetching components (fetch on mount, hide on error) work well for optional UI sections that depend on backend endpoints not yet built; the card gracefully degrades — returns null when API 404s
  - Traps: Backend endpoints for price comparison (US-022/US-023) don't exist yet — the card will remain hidden until those are implemented; ensure the PriceComparisonData interface matches whatever schema US-023 eventually produces
  - Helpful context: Card uses green-50/green-200/green-700 palette to match the app's green accent color scheme; coverage text format matches PRD spec ("השוואה על X מתוך Y מוצרים (Z%)"); PriceComparisonCard is stateless regarding list data — it fetches independently on mount
---

## Iteration 17 — US-017: PDF text extraction utility
- What was done: Created PDF text extraction utility using PyMuPDF 1.27.2.2. Module at app/utils/pdf.py with three functions: extract_text_from_pdf() opens PDF from bytes, iterates pages with sort=True for reading order, returns cleaned text; clean_hebrew_text() normalizes Unicode (NFC), strips control characters, removes unexpected chars, collapses whitespace; has_hebrew_content() checks for Hebrew Unicode ranges. Proper error handling via ReceiptParsingError for invalid PDF, empty PDF, and no-text (scanned image) cases. 22 tests covering extraction (single/multi page, invalid PDF, empty PDF, max_pages limit, empty bytes), cleaning (Hebrew+digits, control chars, whitespace collapse, newlines, line stripping, Unicode normalization, receipt symbols, mixed Hebrew-Latin), and Hebrew detection (Hebrew, Latin, mixed, empty, digits, presentation forms).
- Files affected:
  - backend/app/utils/__init__.py (new) — empty package init
  - backend/app/utils/pdf.py (new) — extract_text_from_pdf, clean_hebrew_text, has_hebrew_content
  - backend/tests/test_pdf.py (new) — 22 tests across 3 test classes
- Learnings for next cycles:
  - Patterns: pymupdf.open(stream=bytes, filetype="pdf") for in-memory PDF processing; page.get_text("text", sort=True) reorders blocks top-left to bottom-right which helps with receipt column layouts; Unicode NFC normalization handles composed vs decomposed Hebrew with nikud
  - Traps: PyMuPDF is fully untyped — all calls need `type: ignore[no-untyped-call]` for mypy strict; helv font in test PDFs doesn't render Hebrew glyphs so can't test real Hebrew extraction without a real Hebrew PDF; RTL text from PyMuPDF may need bidi algorithm for display but for AI parsing the raw extraction is sufficient
  - Helpful context: PyMuPDF 1.27.2.2 installed in smart-shopping conda env; 165 total tests pass (22 new + 143 existing); Hebrew Unicode range \u0590-\u05FF plus presentation forms \uFB1D-\uFB4F; receipt-allowed regex preserves ₪ currency symbol
---

## Iteration 18 — US-018: Claude AI receipt parser service
- What was done: Created receipt parser service using Claude Sonnet (claude-sonnet-4-20250514) with structured Hebrew prompt. Service takes raw receipt text, sends to Claude with a detailed prompt specifying expected JSON format (store_name, store_branch, receipt_date, total_amount, items array). Returns ParsedReceipt/ParsedItem dataclasses with proper Decimal handling. Includes JSON validation (_validate_and_build), markdown code fence stripping, retry logic (max 2 retries on both JSON parse errors and API errors), and proper error hierarchy (ReceiptParsingError for parse failures, ClaudeAPIError for API failures). 24 tests covering: _safe_decimal unit tests (6), _validate_and_build validation (6), valid JSON parsing with store details (1), markdown fence handling (1), invalid JSON retry exhaustion (1), invalid-then-valid retry success (1), empty/whitespace receipt (2), no API key (1), API failure with retries (1), API error then success on retry (1), prompt content verification (1), dataclass defaults (2).
- Files affected:
  - backend/app/services/receipt_parser.py (new) — parse_receipt async function, ParsedReceipt/ParsedItem dataclasses, _validate_and_build, _safe_decimal, RECEIPT_PARSE_PROMPT
  - backend/tests/test_receipt_parser.py (new) — 24 tests across 8 test classes
- Learnings for next cycles:
  - Patterns: Use dataclasses (not Pydantic) for internal service return types — simpler and sufficient when not serializing to API responses; strip markdown ```json fences from Claude responses as it sometimes wraps JSON output; _safe_decimal with Decimal(str(value)) handles float precision issues
  - Traps: Claude sometimes returns JSON wrapped in markdown code fences (```json ... ```) even when instructed not to — always strip these; retry loop needs `range(1, MAX_RETRIES + 2)` for 1 initial + N retries (off-by-one trap); SimpleNamespace works well for mocking Claude response objects in tests
  - Helpful context: 189 total tests pass (24 new + 165 existing); receipt parser follows same patterns as categorizer.py (AsyncAnthropic, structlog, get_settings); prompt is in Hebrew for best results with Hebrew receipt text; MAX_TOKENS=4096 for receipt parsing (vs 100 for categorization) since receipts can have many items
---

## Iteration 19 — US-019: Receipt API endpoints
- What was done: Created receipt API with 3 endpoints: POST /api/v1/receipts/upload (accept PDF, validate magic bytes + 10MB limit, extract text via PyMuPDF, parse with Claude, save Receipt + Purchase records, return parsed result), GET /api/v1/receipts/{id} (full receipt with purchases via selectinload), GET /api/v1/receipts (paginated list with count + offset). All endpoints user-scoped via get_current_user. PriceHistory update deferred to US-020/US-022 when product matching is implemented. 19 tests covering: file validation (non-PDF, oversized), upload flow with mocked extraction+parsing (success, invalid date), get receipt (success, not found, user isolation), list receipts (empty, with data, pagination), auth protection for all 3 endpoints, _validate_pdf unit tests (valid, non-PDF, oversized, empty), _build_purchases_from_parsed unit tests (correct count, empty items).
- Files affected:
  - backend/app/api/v1/receipt.py (new) — 3 endpoints, 6 Pydantic schemas, PDF validation helper, purchase builder helper
  - backend/app/api/v1/__init__.py (updated) — added receipt_router
  - backend/tests/test_receipt.py (new) — 19 tests across 7 test classes
- Learnings for next cycles:
  - Patterns: Use selectinload(Receipt.purchases) for eager loading related purchases in single-receipt endpoint; track objects added to mock session with side_effect to assign fake IDs/timestamps for response serialization; UploadFile from FastAPI handles file reading with await file.read()
  - Traps: Receipt.status is a string column, not enum — set to "parsed" after successful parsing (default "pending"); parsed.receipt_date is a string from Claude that needs date.fromisoformat() conversion with try/except fallback; PriceHistory update requires product matching (US-020) so it's deferred — the Receipt+Purchase records are saved without product_id links
  - Helpful context: 208 total tests pass (19 new + 189 existing); mypy strict passes; receipt router at /api/v1/receipts; PDF validation checks %PDF magic bytes prefix; max file size 10MB; pagination uses count query + offset/limit pattern
---

## Iteration 20 — US-020: Product matching from receipts
- What was done: Created product_matcher.py service with rapidfuzz Hebrew fuzzy matching. Match priority chain: barcode exact → normalized name exact → fuzzy match (threshold 0.85) → create new product. Added normalize_hebrew_name() that strips nikud, normalizes Unicode NFC, lowercases Latin chars, removes punctuation, and collapses whitespace. When purchases match list items (by product_id or fuzzy name), items are marked as completed with source="receipt" and refresh frequencies recalculated. Added PATCH /api/v1/list/items/{id}/upgrade endpoint to upgrade generic names to precise receipt names (updates both ListItem and linked Product). Integrated matching into receipt upload flow — match_receipt_purchases is called after purchases are saved, returning match_counts in the upload response. Updated existing receipt upload tests to mock the matcher. 18 new tests covering: normalization (6), barcode match (2), exact name match (1), fuzzy match above/below threshold (2), new product creation (1), upgrade endpoint (4), receipt matching integration (2).
- Files affected:
  - backend/app/services/product_matcher.py (new) — normalize_hebrew_name, match_purchase_to_product, match_receipt_purchases, _complete_matching_list_items
  - backend/app/api/v1/list.py (updated) — added UpgradeItemRequest schema, PATCH /items/{id}/upgrade endpoint
  - backend/app/api/v1/receipt.py (updated) — integrated match_receipt_purchases into upload flow, added MatchCountsResponse schema
  - backend/tests/test_product_matcher.py (new) — 18 tests across 8 test classes
  - backend/tests/test_receipt.py (updated) — added mock for match_receipt_purchases in upload tests
- Learnings for next cycles:
  - Patterns: rapidfuzz.fuzz.token_sort_ratio works well for Hebrew product names where word order may differ; normalize names by stripping nikud (\u0591-\u05BD range) before comparison; mock async service calls at the import point in the module being tested (e.g., patch("app.api.v1.receipt.match_receipt_purchases"))
  - Traps: When integrating a new async service into an existing flow with mocked tests, existing tests break because the mock session doesn't support the new queries — must mock the new service in existing tests; receipt.purchases needs to be explicitly set before calling match_receipt_purchases since purchases were just created individually
  - Helpful context: 226 total tests pass (18 new + 208 existing); mypy strict passes on all 35 source files; FUZZY_THRESHOLD = 85.0; upgrade endpoint updates both ListItem.name and Product.name+normalized_name; match_counts response includes barcode/exact_name/fuzzy/new/completed_items counts
---

## Iteration 21 — US-021: Receipt upload UI with parsed results
- What was done: Created full receipt upload UI with three components. ReceiptUpload.tsx uses react-dropzone for drag-drop/tap PDF selection (10MB limit, PDF-only validation, drag-active/rejected states with Hebrew messages). ReceiptResults.tsx displays parsed receipt results — receipt header card (store name, branch, date, total in green), match summary card (completed items count, barcode/exact/fuzzy breakdown), orange warning for unmatched items, collapsible category breakdown (matched vs new products), parsed items list with per-item delete and match status indicators (green check for matched, orange triangle for unmatched), and "אישור ושמירה" green button that navigates to list tab. ReceiptsPage.tsx orchestrates the full flow with three views (upload/uploading/results): upload zone with react-dropzone, loading skeleton with spinner while Claude parses, results display, and receipt history list grouped by Hebrew month labels. FormData upload with multipart/form-data header for the PDF.
- Files affected:
  - frontend/src/components/ReceiptUpload.tsx (new) — react-dropzone drag-drop/tap upload with PDF validation
  - frontend/src/components/ReceiptResults.tsx (new) — parsed items display, match summary, category breakdown, save button
  - frontend/src/pages/ReceiptsPage.tsx (updated) — full receipt flow: upload → skeleton → results, receipt history by month
- Learnings for next cycles:
  - Patterns: react-dropzone accept prop uses MIME type map { "application/pdf": [".pdf"] }; FormData + "Content-Type": "multipart/form-data" header for file upload via Axios; groupByMonth helper with toLocaleDateString("he-IL", { month: "long", year: "numeric" }) for Hebrew month labels
  - Traps: Backend upload already saves everything (receipt + purchases + product matches) — "אישור ושמירה" button is just UX confirmation, not a separate save API call; showToast must be imported from Toast component for non-React error handling in upload flow
  - Helpful context: TypeScript typecheck passes; three-state view pattern (upload/uploading/results) keeps the page clean; ReceiptResults allows client-side purchase deletion (filters from local state) for items user wants to exclude before confirming
---

## Iteration 16 — US-022: SuperGET API client and product matching
- What was done: Created SuperGET API client service with barcode-first/name-fallback product matching, price history persistence with 24-hour dedup, retry logic, and graceful degradation on API failure. 34 tests covering all layers.
- Files affected:
  - backend/app/services/price_comparator.py (new) — SuperGET API client, product price lookup, PriceHistory save
  - backend/tests/test_price_comparator.py (new) — 34 tests: unit (_safe_decimal, _parse_store_prices), async (API requests, search functions, matching priority, save to history, batch fetch)
- Learnings for next cycles:
  - Patterns: SuperGET API uses POST to https://api.superget.co.il with action + api_key params; response may use 'data', 'results', or 'items' keys for results array; price field may be 'price' or 'item_price'; store may be 'chain_name' or 'store_name'
  - Traps: SuperGET website appears to be down (DNS resolution fails) as of 2026-03 — the client is built from documented API patterns but cannot be live-tested; AsyncMock with spec_set breaks db.execute awaiting — use plain AsyncMock() for mock DB sessions; db.add() with AsyncMock triggers "coroutine never awaited" warning but tests pass
  - Helpful context: 260 total tests pass (34 new + 226 existing); mypy strict passes on all 36 source files; superget_api_key already exists in config.py Settings; SuperGETError already defined in core/errors.py; price_comparator follows same patterns as receipt_parser (dataclasses, structlog, get_settings, retry logic)
---

## Iteration 23 — US-023: Price comparison endpoints
- What was done: Created basket comparison service and two API endpoints (compare-receipt, compare-list) that calculate total basket cost per supermarket chain using PriceHistory data, with coverage indicators and savings calculation
- Files affected:
  - backend/app/services/basket_comparator.py (new) — basket comparison logic: latest price per product per store via SQL subquery, aggregate totals, rank cheapest, calculate savings
  - backend/app/api/v1/price.py (new) — two endpoints: GET /api/v1/prices/compare-receipt/{id} and GET /api/v1/prices/compare-list, with Hebrew coverage text
  - backend/app/api/v1/__init__.py (updated) — registered price router
  - backend/tests/test_price.py (new) — 12 tests: basket calculation (empty, no data, full coverage, partial, current store savings), receipt comparison (not found, with purchases, no matched), list comparison (with items, empty), coverage text (partial, full)
  - PRD.md (updated) — marked US-023 complete
- Learnings for next cycles:
  - Patterns: Use tuple indexing (row[0], row[1]) for SQLAlchemy result rows instead of named attributes to avoid mypy union-attr issues; SQL subquery with func.max for "latest per group" pattern; response schema matches frontend PriceComparisonCard interface exactly (comparisons, total_items, matched_items, cheapest_store, cheapest_total, current_total, savings)
  - Traps: MagicMock attributes return MagicMock objects which can't be compared with < operator — use plain tuples for DB row mocks in unit tests; list[StoreBasket] is not assignable to list[object] in mypy strict (invariance) — use the concrete type
  - Helpful context: 272 total tests pass (12 new + 260 existing); mypy strict passes on all 38 source files; frontend PriceComparisonCard.tsx already calls GET /api/v1/prices/compare-list — this endpoint now fulfills that contract; coverage_text in Hebrew with percentage for partial matches
---

## Iteration 14 — US-024: Dashboard backend API
- What was done: Created dashboard API router with three analytics endpoints: spending by category, spending by store, and monthly spending trends. All endpoints are user-scoped via get_current_user dependency. Registered router in api_v1_router.
- Files affected:
  - backend/app/api/v1/dashboard.py (new) — three GET endpoints: /spending, /stores, /trends with Pydantic response models
  - backend/app/api/v1/__init__.py (updated) — registered dashboard_router
  - backend/tests/test_dashboard.py (new) — 12 tests covering all endpoints, empty states, period validation, Hebrew fallback labels
  - PRD.md (updated) — marked US-024 complete
- Learnings for next cycles:
  - Patterns: Use tuple indexing (row[0], row[1]) for SQLAlchemy aggregate query results in tests; func.coalesce for nullable fields with Hebrew fallback labels ("אחר", "לא ידוע"); Query parameter validation with pattern regex rejects invalid values with 422
  - Traps: func.to_char is PostgreSQL-specific — works for prod but mock tests bypass actual SQL execution so this is fine; Decimal(str(row[1])) needed for sum of mock Decimal values to avoid float precision issues
  - Helpful context: 284 total tests pass (12 new + 272 existing); mypy strict passes on all 39 source files; dashboard endpoints at /api/v1/dashboard/*; spending endpoint joins Purchase→Receipt→Product→Category for category breakdown; trends uses last 12 months window
---

## Iteration 25 — US-025: Dashboard frontend UI with charts
- What was done: Created DashboardPage.tsx with spending total card (green gradient), category donut chart (recharts PieChart with inner radius for donut style, custom percentage labels, color legend), store breakdown (horizontal progress bars with receipt count), and monthly trend line chart (recharts LineChart with Hebrew month labels, RTL axes). Added period selector (week/month/year) that re-fetches spending data. Updated MorePage.tsx from stub to iOS Settings-style menu with "דשבורד הוצאות" link using BarChart3 icon and ChevronLeft arrow. Added /dashboard route to App.tsx. Empty state shows helpful message to upload receipts. All data fetched from three dashboard API endpoints in parallel with AbortController cleanup.
- Files affected:
  - frontend/src/pages/DashboardPage.tsx (new) — full dashboard with 4 sections: spending total, category donut, store breakdown, monthly trend
  - frontend/src/pages/MorePage.tsx (updated) — iOS Settings-style menu with dashboard link
  - frontend/src/App.tsx (updated) — added DashboardPage import and /dashboard route
  - PRD.md (updated) — marked US-025 criteria complete
- Learnings for next cycles:
  - Patterns: recharts ResponsiveContainer needs explicit height; PieChart with innerRadius creates donut; XAxis reversed prop for RTL month ordering; YAxis orientation="right" for RTL layout; Promise.all for parallel API fetching
  - Traps: recharts custom label function receives cx/cy/midAngle/innerRadius/outerRadius — need to calculate position manually with trigonometry; hide labels for small slices (< 5%) to avoid overlap
  - Helpful context: TypeScript typecheck passes clean; recharts 2.15 installed; CATEGORY_COLORS array of 15 colors matches the 15 Hebrew categories; formatCurrency uses ₪ prefix with toFixed(0) for clean display
---

## Iteration 26 — US-026: More page, settings, and category management UI
- What was done: Updated MorePage.tsx to iOS Settings-style grouped menu with 5 items (דשבורד הוצאות, ניהול קטגוריות, הגדרות, עזרה ומשוב, התנתק) organized in 3 visual groups with colored icon backgrounds. Created SettingsPage.tsx with account info display (name, email from authStore) and app version. Created CategoryManagementPage.tsx with full CRUD: inline rename with input focus, add new category with inline input, delete with confirmation dialog (items moved to אחר), and HTML5 drag-and-drop reorder that calls POST /api/v1/categories/reorder. Added /settings and /categories routes to App.tsx.
- Files affected:
  - frontend/src/pages/MorePage.tsx (updated) — iOS Settings grouped menu with MenuGroup, MenuItem, MenuDivider components, logout via authStore
  - frontend/src/pages/SettingsPage.tsx (new) — basic settings with account info and version
  - frontend/src/pages/CategoryManagementPage.tsx (new) — full category CRUD with drag reorder
  - frontend/src/App.tsx (updated) — added SettingsPage and CategoryManagementPage routes
  - PRD.md (updated) — marked US-026 criteria complete
- Learnings for next cycles:
  - Patterns: MenuGroup/MenuItem/MenuDivider pattern creates iOS Settings-style grouped lists cleanly; HTML5 draggable + onDragStart/onDragOver/onDrop provides basic reorder without extra deps; useRef + useEffect for auto-focus on inline edit inputs
  - Traps: Array.splice returns T|undefined in strict TS — need explicit guard before using the spliced element; showToast imported from Toast component for non-React contexts
  - Helpful context: TypeScript typecheck passes clean; backend category endpoints already support all needed operations (GET, POST, PUT, DELETE, POST /reorder); authStore exposes logout() for disconnect button
---

## Iteration 27 — US-027: Onboarding flow
- What was done: Transformed single-screen OnboardingPage into 3-step onboarding flow. Step 1: logo + tagline + Google Sign-In (preserved existing implementation). Step 2: three swipeable value cards with dot indicators, touch swipe support for RTL (swipe left=next), animated active dot (wider green pill), navigation arrows, "המשך" button on last card, "דלג" skip link. Step 3: "מה תרצה לעשות?" with two action cards — "העלאת קבלה" (→ /receipts) and "יצירת רשימה" (→ /list), each with icon, title, and description. Added onboardingComplete flag to authStore (persisted in localStorage as "smartkal_onboarded") so returning users skip steps 2-3 after re-login. Updated App.tsx to show onboarding for both unauthenticated users and authenticated users who haven't completed onboarding.
- Files affected:
  - frontend/src/pages/OnboardingPage.tsx (rewritten) — 3-step flow with StepLogin, StepValueCards, StepChooseAction components
  - frontend/src/store/authStore.ts (updated) — added onboardingComplete state + completeOnboarding action + localStorage persistence
  - frontend/src/App.tsx (updated) — added onboardingComplete check to routing logic
  - PRD.md (updated) — marked US-027 criteria complete
- Learnings for next cycles:
  - Patterns: Use localStorage for non-sensitive UX flags (onboarding completion) even when auth tokens are in-memory only; React.TouchEvent touch array elements may be undefined in strict TS — guard with optional access; `as const` on arrays makes elements readonly which helps with type inference but requires fallback for array indexing
  - Traps: TypeScript strict mode treats array index access as possibly undefined — use nullish coalescing (`??`) for array element access; RTL swipe direction: positive touchStart-touchEnd diff means swipe left (which should go to next card in RTL)
  - Helpful context: TypeScript typecheck passes clean; lucide-react provides RefreshCw, Brain, Upload, ListPlus icons used in onboarding cards; onboarding localStorage key is "smartkal_onboarded"
---
